{
  "summary": "Create a new <%- name %>",
  "description": "",
  "operationId": "<%- name %>.create",
  "parameters": [
    {
      "description": "The <%- name %> to be created, omitting <% if (typeof id !== 'undefined' && id === '_id' && idIsObjectId) { %>the unique identifier (that will be generated by the server) and <% } %>the metadata",
      "name": "body",
      "in": "body",
      "required": true,
      "schema": <%- JSON.stringify(schemaWrite || schema) %>
    }
  ],
  "responses": {
    "201": {
      "description": "<%- name %> successfully created",
      "schema": <%- JSON.stringify(schema) %>,
      "headers": {
        "Location": {
          "description": "URI of the newly created resource",
          "type": "string",
          "format": "uri"
        }
      }
    },
    "default": { "$ref": "#/responses/defaultError" }
  }
}
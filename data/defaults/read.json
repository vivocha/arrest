{
  "summary": "Retrieve a <%- name %> by id",
  "operationId": "<%- name %>.read",
  "parameters": [
    {
      "$ref": "#/parameters/id"
    },
    {
      "$ref": "#/parameters/fields"
    }
  ],
  "responses": {
    "200": {
      "description": "The requested <%- name %>",
      "schema": <%- getItemSchema('read') %>
    },
    "404": { "$ref": "#/responses/notFound" },
    "default": { "$ref": "#/responses/defaultError" }
  }
}
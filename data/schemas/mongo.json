{
  "title": "A JSON Schema for an API resource stored in a MongoDB collection.",
  "id": "https://raw.githubusercontent.com/vivocha/arrest/master/data/schemas/mongo.json#",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "allOf": [
    { "$ref": "https://raw.githubusercontent.com/vivocha/arrest/master/data/schemas/resource.json#" }
  ],
  "required": [ "db" ],
  "properties": {
    "db": {
      "description": "A MongoDB URI or a MongoClient instance",
      "anyOf": [
        {
          "type": "object"
        },
        {
          "type": "string",
          "format": "uri"
        }
      ]
    },
    "collection": {
      "description": "The MongoDB collection to use. If omitted, _namePlural_ converted to lowercase is used",
      "type": "string",
      "minLength": 1
    },
    "idIsObjectId": {
      "description": "Flag indicating whether the _id_ is to be treated as a MongoDB ObjectID",
      "type": "boolean",
      "default": true
    }
  }
}